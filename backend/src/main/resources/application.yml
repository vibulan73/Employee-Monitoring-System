spring:
  application:
    name: employee-monitoring-backend
  
  # # Default profile (H2 for development)
  # datasource:
  #   url: jdbc:h2:file:./data/monitoring;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password: 

   # Default profile (H2 for development)
  datasource:
    url: ${DB_URL}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  
  jpa:
    hibernate:
      ddl-auto: update
    # show-sql: true
    properties:
      hibernate:
        # format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  # File upload configuration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # Email Configuration (SMTP - Deprecated for Gmail API)
  # mail:
  #   host: smtp.gmail.com
  #   port: 465
  #   username: ${MAIL_USERNAME}
  #   password: ${MAIL_PASSWORD}
  #   properties:
  #     mail:
  #       debug: true
  #       smtp:
  #         auth: true
  #         ssl:
  #           enable: true
  #           trust: smtp.gmail.com
  #         connectiontimeout: 60000
  #         timeout: 60000
  #         writetimeout: 60000

  # Gmail API Configuration
  gmail:
    credentials:
      file-path: ${GMAIL_CREDENTIALS_PATH}
    tokens:
      directory-path: ${GMAIL_TOKENS_PATH}

server:
  port: ${PORT}

# Application specific configuration
monitoring:
  screenshot:
    storage-path: ${SCREENSHOT_PATH}
  cors:
    allowed-origins: ${ALLOWED_ORIGINS}
    allowed-methods: GET,POST,PUT,DELETE,OPTIONS
    allowed-headers: "*"
    allow-credentials: true
  admin:
    email: ${ADMIN_EMAIL}
    cc-email: ${ADMIN_CC_EMAIL}
    bcc-email: ${ADMIN_BCC_EMAIL}
  idle:
    warning-minutes: 1
    auto-stop-minutes: 2
    check-interval-minutes: 1

# Application timezone for login rule evaluation
app:
  timezone: Asia/Colombo  # Sri Lanka Standard Time

# Logging
logging:
  level:
    com.monitoring: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO
