spring:
  application:
    name: employee-monitoring-backend
  
  # # Default profile (H2 for development)
  # datasource:
  #   url: jdbc:h2:file:./data/monitoring;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password: 

   # Default profile (H2 for development)
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/monitordb}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:4321}
  
  jpa:
    hibernate:
      ddl-auto: update
    # show-sql: true
    properties:
      hibernate:
        # format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  # File upload configuration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # Email Configuration (SMTP - Deprecated for Gmail API)
  # mail:
  #   host: smtp.gmail.com
  #   port: 465
  #   username: ${MAIL_USERNAME:vibulan73@gmail.com}
  #   password: ${MAIL_PASSWORD:zmnj ritu exdw ykau}
  #   properties:
  #     mail:
  #       debug: true
  #       smtp:
  #         auth: true
  #         ssl:
  #           enable: true
  #           trust: smtp.gmail.com
  #         connectiontimeout: 60000
  #         timeout: 60000
  #         writetimeout: 60000

  # Gmail API Configuration
  gmail:
    credentials:
      file-path: ${GMAIL_CREDENTIALS_PATH:src/main/resources/credentials.json}
    tokens:
      directory-path: ${GMAIL_TOKENS_PATH:tokens}

server:
  port: ${PORT:8080}

# Application specific configuration
monitoring:
  screenshot:
    storage-path: ${SCREENSHOT_PATH:./screenshots}
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
    allowed-methods: GET,POST,PUT,DELETE,OPTIONS
    allowed-headers: "*"
    allow-credentials: true
  admin:
    email: ${ADMIN_EMAIL:vibulan73@gmail.com,sdogi.uoc@gmail.com,vibulan.20@cse.mrt.ac.lk}
    # cc-email: ${ADMIN_CC_EMAIL:vibulan.20@cse.mrt.ac.lk,vibulan2000@gmail.com}
    # bcc-email: ${ADMIN_BCC_EMAIL:pirathees2007@gmail.com}
  idle:
    warning-minutes: 1
    auto-stop-minutes: 2
    check-interval-minutes: 1

# Application timezone for login rule evaluation
app:
  timezone: Asia/Colombo  # Sri Lanka Standard Time

# Logging
logging:
  level:
    com.monitoring: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO
